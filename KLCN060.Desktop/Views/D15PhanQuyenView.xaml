<UserControl x:Class="KLCN060.Desktop.Views.D15PhanQuyenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28" Background="#F5F6F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Phân quyền" FontSize="26" FontWeight="Bold" Foreground="#123328" />

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="20" Background="White" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Text="Vai trò và quyền" FontSize="18" FontWeight="SemiBold" />
                        <Button Margin="16,0,0,0" Content="Sửa quyền" Command="{Binding MoSuaVaiTroCommand}" />
                    </StackPanel>
                    <DataGrid Grid.Row="1"
                              Margin="0,14,0,0"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              ItemsSource="{Binding VaiTro}"
                              SelectedItem="{Binding VaiTroDaChon}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="160" Header="Vai trò" Binding="{Binding TenVaiTro}" />
                            <DataGridTemplateColumn Width="*" Header="Quyền đã gán">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding Quyen}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,2,5,2" Padding="6,2" Background="#E4EDE1" CornerRadius="8">
                                                        <TextBlock FontSize="11" Text="{Binding TenQuyen}" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="2" Padding="20" Background="White" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <TextBlock Text="Vai trò nhân viên" FontSize="18" FontWeight="SemiBold" />
                    <DataGrid Grid.Row="1" Margin="0,14,0,0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False" ItemsSource="{Binding NhanVien}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="110" Header="Mã NV" Binding="{Binding MaNV}" IsReadOnly="True" />
                            <DataGridTextColumn Width="*" Header="Họ tên" Binding="{Binding HoTen}" IsReadOnly="True" />
                            <DataGridTextColumn Width="150" Header="Vai trò hiện tại" Binding="{Binding TenVaiTroHienTai}" IsReadOnly="True" />
                            <DataGridTemplateColumn Width="180" Header="Đổi vai trò">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Height="30"
                                                  ItemsSource="{Binding DataContext.VaiTro, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  DisplayMemberPath="TenVaiTro"
                                                  SelectedValuePath="MaVaiTro"
                                                  SelectedValue="{Binding MaVaiTroMoi, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="100" Header="">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Cập nhật"
                                                Command="{Binding DataContext.DoiVaiTroCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Margin="0,14,0,0" Foreground="#C8452F" Text="{Binding ThongBao}" TextAlignment="Center" TextWrapping="Wrap" />

        <Border Grid.RowSpan="3" Background="#80000000">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers><DataTrigger Binding="{Binding DangSuaVaiTro}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                </Style>
            </Border.Style>
            <Border Width="460" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <TextBlock Text="Sửa quyền vai trò" FontSize="20" FontWeight="Bold" Foreground="#123328" />
                    <TextBlock Grid.Row="1" Margin="0,8,0,12" Text="{Binding VaiTroDaChon.TenVaiTro}" />
                    <ListBox Grid.Row="2" BorderThickness="0" ItemsSource="{Binding Quyen}">
                        <ListBox.ItemTemplate><DataTemplate><CheckBox Margin="0,5" IsChecked="{Binding DuocChon}" Content="{Binding TenQuyen}" /></DataTemplate></ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Grid.Row="3" Margin="0,16,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <Button Content="Hủy" Command="{Binding DongSuaVaiTroCommand}" />
                        <Button Content="Lưu quyền" Command="{Binding LuuQuyenCommand}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
