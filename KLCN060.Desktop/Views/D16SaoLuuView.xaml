<UserControl x:Class="KLCN060.Desktop.Views.D16SaoLuuView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28" Background="#F5F6F0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Sao lưu và phục hồi dữ liệu" FontSize="26" FontWeight="Bold" Foreground="#123328"/>
        <StackPanel Grid.Row="1" Margin="0,18,0,12" Orientation="Horizontal"><Button Content="Sao lưu ngay" Command="{Binding SaoLuuNgayCommand}"/><TextBlock Margin="14,0,0,0" VerticalAlignment="Center" Foreground="#C8452F" Text="{Binding KetQua}" TextWrapping="Wrap"/></StackPanel>
        <Border Grid.Row="2" Padding="20" Background="White" CornerRadius="10"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="Lịch sử sao lưu" FontSize="18" FontWeight="SemiBold"/><DataGrid Grid.Row="1" Margin="0,14,0,0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding LichSu}"><DataGrid.Columns><DataGridTextColumn Header="Thời gian" Width="150" Binding="{Binding ThoiGianThucHien, StringFormat=dd/MM/yyyy HH:mm}"/><DataGridTextColumn Header="Loại thao tác" Width="130" Binding="{Binding LoaiThaoTac}"/><DataGridTextColumn Header="Kết quả" Width="110" Binding="{Binding KetQua}"/><DataGridTextColumn Header="Đường dẫn file" Width="*" Binding="{Binding DuongDanFile}"/><DataGridTemplateColumn Header="" Width="100"><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="Phục hồi" Command="{Binding DataContext.PhucHoiCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"><Button.Style><Style TargetType="Button"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><MultiDataTrigger><MultiDataTrigger.Conditions><Condition Binding="{Binding LoaiThaoTac}" Value="SAO_LUU"/><Condition Binding="{Binding KetQua}" Value="THANH_CONG"/></MultiDataTrigger.Conditions><Setter Property="Visibility" Value="Visible"/></MultiDataTrigger></Style.Triggers></Style></Button.Style></Button></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns></DataGrid></Grid></Border>
    </Grid>
</UserControl>
