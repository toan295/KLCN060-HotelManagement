<UserControl x:Class="KLCN060.Desktop.Views.D13QuanLyDichVuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F5F6F0">
        <Grid Margin="28">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="Quản lý danh mục dịch vụ" FontSize="26" FontWeight="Bold" Foreground="#123328" />
                <TextBlock Text="Quản lý các dịch vụ cung cấp tại khách sạn." Margin="0,5,0,0" Foreground="#647A6C" />
            </StackPanel>
            <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="18" Margin="0,18,0,0" BorderBrush="#D7DDCE" BorderThickness="1">
                <WrapPanel>
                    <StackPanel><TextBlock Text="Tên dịch vụ" Foreground="#3C4C40" /><TextBox Width="200" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                    <StackPanel><TextBlock Text="Đơn giá" Foreground="#3C4C40" /><TextBox Width="120" Text="{Binding Price}" /></StackPanel>
                    <StackPanel><TextBlock Text="Đơn vị tính" Foreground="#3C4C40" /><TextBox Width="130" Text="{Binding Unit}" /></StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"><Button Content="Lưu" Command="{Binding SaveCommand}" /><Button Content="Xóa" Command="{Binding DeleteCommand}" Background="#C8452F" BorderBrush="#C8452F" /><Button Content="Làm mới" Command="{Binding ClearCommand}" Background="White" Foreground="#1F4D3D" BorderBrush="#1F4D3D" /></StackPanel>
                </WrapPanel>
            </Border>
            <Grid Grid.Row="2" Margin="0,14,0,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                <TextBlock Text="{Binding Message}" Foreground="#C8452F" TextWrapping="Wrap" />
                <DataGrid Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}" AutoGenerateColumns="False" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã dịch vụ" Binding="{Binding MaDV}" Width="100" />
                        <DataGridTextColumn Header="Tên dịch vụ" Binding="{Binding TenDV}" Width="*" />
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding GiaDV, Converter={StaticResource VndMoneyConverter}}" Width="150" />
                        <DataGridTextColumn Header="Đơn vị tính" Binding="{Binding DonViTinh}" Width="180" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
